---
title: 'Dream Big'
publishedAt: '2026-01-01'
summary: 'Dream Big by Shobith Mallya'
---

![Dream Big Header Image](dream-big-thumbnail.png)

This is an example post that you can access at `/example` on your domain.

You can write **markdown** here and it will be rendered just like your blog posts!

## Features

- Same MDX support as blog posts
- Accessible at the root level (e.g., `yoursite.com/example`)
- Uses the same styling and layout


This is an example post that you can access at `/example` on your domain.

You can write **markdown** here and it will be rendered just like your blog posts!

## Features

- Same MDX support as blog posts
- Accessible at the root level (e.g., `yoursite.com/example`)
- Uses the same styling and layout

This is an example post that you can access at `/example` on your domain.

You can write **markdown** here and it will be rendered just like your blog posts!

## Features

- Same MDX support as blog posts
- Accessible at the root level (e.g., `yoursite.com/example`)
- Uses the same styling and layout

This is an example post that you can access at `/example` on your domain.

You can write **markdown** here and it will be rendered just like your blog posts!

## Features

- Same MDX support as blog posts
- Accessible at the root level (e.g., `yoursite.com/example`)
- Uses the same styling and layout
